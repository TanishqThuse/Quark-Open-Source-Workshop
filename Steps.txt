Some basics linux commads -> helpful for git bash
Linux commands for git like cd, ls, mkdir, pwd, Readme syntax, touch, mkdir , man

After installing git for the first time you have to tell git who you are  beore you can commit

1) git config --global user.name "MyName"
2) git config --global user.email "MyEmailId@gmail.com"

Now we can start actually executing the commands
3) git clone
4) git status
     a)untracked
     b)changed
     c)staged to commit
     d)unchanged

5) git add <file-name>
6) git commit -m "MESSAGE" #MAKE sure the message is meaningfull
7) Make some changes in the repo and do it
8) git push origin main

9)git log,
gitignore,
gitkeep -> place inside an empty directory

10)Git restore -> new changes made will be gone forever and previous commit stage be restored
and reset ->
, hard

11)Git stash

12)Branches, master, main, head, checkout
Merging branch to main/master
Merge Requests

   --Create a new branch
   --make changes
   --show two brnaches on github
   --merge

13) Verifiedi commits on github

15) Steps to get a certificate :
    1) Create a fork of the TeamQuark Open Source Workshop repo
    2) *Create a new branch* -> give name as YOUR_NAME
    3) Make changes in the newly created branch (after forking)
    4) make changes in readme -> git add README.md
    5) git commit -m "Adding my name""
    6) Make a *verified commit*
    7)  go to quark's repo and
        --create a pull request

Checkout Imp_points file
